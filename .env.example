# Copy to root .env only if .env does not exist. Never overwrite existing values.
# Server-only secret. PLAN.md requires Potens as the only AI provider.
POTENS_API_KEY=""

# Legacy server fallback remains in code until functional normalization.
# Leave empty; do not activate Gemini as part of folder setup.
GEMINI_API_KEY=""

# Legacy hosting setting; configure only if required by the actual environment.
APP_URL=""

# Browser-visible Supabase values. Never put a service-role/secret key in VITE_*.
VITE_SUPABASE_URL="https://YOUR_SUPABASE_PROJECT_ID.supabase.co"
VITE_SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY"
SUPABASE_URL="https://YOUR_SUPABASE_PROJECT_ID.supabase.co"
SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY"

# Keep empty until consent-before-loading is implemented and verified (PLAN.md).
# Existing GA4 code does not yet meet that contract.
VITE_GA4_ID=""
