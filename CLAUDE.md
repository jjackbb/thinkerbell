# 니편내편 — 작업 규칙

## 커밋할 때마다 PRD.md에 기록한다

**커밋을 만들면 `PRD.md` §7 패치 노트에 항목을 하나 추가한다.** 팀원이 그 파일만 보고도
지금까지 어떤 패치가 있었는지 파악할 수 있어야 한다.

형식 (최신 것이 위로):

```
### PATCH-0NN | YYYY-MM-DD | 한 줄 제목

> **요약:** 한 문장. 무엇이 어떻게 바뀌었는지.

- **무엇을:** 실제로 바뀐 것
- **왜:** 이 변경이 필요했던 이유 (증상이 있었다면 증상부터)
- **확인:** 어떻게 검증했는지
- **파일:** 손댄 파일 목록
```

**요약 한 줄은 반드시 넣는다.** 개발자가 아닌 사람이 이 줄만 읽고도 무엇이 바뀌었는지
알 수 있어야 한다. 용어 대신 겪는 일로 쓴다 — "handleVote 수정" 말고 "투표가 저장되지
않던 문제를 고쳤다".

**§7 맨 위 '한눈에 보기' 표에도 한 줄 추가한다.** 그냥 두면 서비스가 잘못 동작하거나
개인정보가 새는 문제였다면 번호에 ⚠️ 를 단다.

`영향` / `정책` / `한계` 항목은 해당될 때만 붙인다. PRD 본문(§1~§6)과 어긋나는 결정을
했다면 **본문도 같이 고치고** 패치 노트에 그 사실을 남긴다.

커밋 후에는 확인받지 않고 바로 `git push` 한다.

## AI 프롬프트

대화 프롬프트는 전부 `src/lib/prompts.ts` 한 곳에 있다. 컴포넌트나 `App.tsx` 안에
프롬프트 문자열을 새로 쓰지 말 것. 자세한 내용은 `docs/ai-prompts.md`.

## 디자인

색·크기는 `src/index.css`의 토큰만 쓴다. 즉흥으로 hex를 정하지 않는다.
