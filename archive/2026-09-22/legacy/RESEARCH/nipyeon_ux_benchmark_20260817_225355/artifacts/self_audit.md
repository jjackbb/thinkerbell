# 니편내편 현재 구현 자체 감사 (코드 직접 확인, 2026-08-17)

> 이 문서의 주장은 전부 저장소 코드를 직접 읽어 확인한 것이다 (claim_type: executable / 검증 방법: grep + 파일 읽기).

## 안티패턴 대조표

| 안티패턴 | 우리 앱 상태 | 근거 (파일:줄) | 판정 |
|---|---|---|---|
| **A1** 익명이 기본값이 아님 | 댓글에 '익명 표시' **체크박스가 존재**. 기본값은 켜짐(`useState(!isMyStory)`)이지만 **끄면 계정 닉네임이 그대로 노출**됨 | `StoryDetailModal.tsx:68,424` | ⚠️ 부분 해당 |
| **A12** 가명 마모 / 재식별 | 사연 카드에 **작성자 닉네임이 그대로 노출**(`story.authorNickname`). 같은 닉네임으로 연애·직장 사연을 여러 개 쓰면 조합으로 특정 가능 | `StoryCard.tsx:105` | 🔴 해당 |
| 익명 번호 안정성 | 댓글 익명번호를 **현재 댓글 수 + 1**로 계산 → 앞선 댓글이 삭제되면 **번호가 중복되거나 밀린다** | `App.tsx:396-401` | 🔴 버그 수준 |
| **A7** 소수 여론 왜곡 | 투표 표본이 1표여도 **100%로 표시**. 최소 표본 처리 없음 (`isZeroVotes`만 분기) | `StoryCard.tsx:77-80,197` | 🔴 해당 |
| **A3** 신고 N회 단순 카운트 | **신고 5회 = 자동 블라인드**, 조회수 대비 비율·조직 신고 탐지·신고자 제재 **전부 없음** | `App.tsx:846,874`, `20260817000000_tighten_rls.sql:248,277` | 🔴 해당 |
| **A4** 소명 경로 부재 | 블라인드 처리된 글에 대한 **이의제기 UI 없음**. 작성자는 왜 가려졌는지 알 수 없음 | (해당 코드 부재로 확인) | 🔴 해당 |

## 잘 되어 있는 것

- 신고/투표/공감 카운터가 **서버 함수(SECURITY DEFINER)로만** 변경 가능 → 클라이언트 위조 차단 (`20260817000000_tighten_rls.sql`)
- 투표 중복 방지가 **DB 기본키((storyId,userId))로 구조적으로** 차단됨 (`20260817020000_votes_table.sql`)
- 본인 사연 투표 금지가 서버에서 강제됨
- 사연 등록 시 AI가 비속어·폭력 표현을 마스킹 (`api/index.ts` moderate-story)
- 19금 게이트 존재 (`AdultVerificationModal`)

## 디자인 시스템 불일치 (별건)

- 문서상 기준: **Clay** (`Todos.md`에 "Clay 디자인 시스템 CSS/토큰 구현" 명시, 노션 자료조사에도 getdesign.md/clay 링크)
  - Clay 토큰: canvas `#fffaf0`(크림), primary `#0a0a0a`, 액센트는 pink/teal/lavender/peach/ochre 다색 카드
- 실제 구현: **에메랄드 + 근검정**
  - 실사용 빈도: `#1C1C1C`(188) · `#3ECF8E`(176) · `#5f5e5e`(120) · `#E5E7EB`(106) · `#f3f4f5`(44) · `#f8f9fa`(31)
  - Clay 잔재는 소수만 남음 (`#0a0a0a` 18, `#f5f0e0` 7, `#e8e2d0` 11)
- **`#3ECF8E`는 Supabase의 브랜드 그린이다** (index.html title도 "니편내편 - Emerald Edition")
  - → 서비스가 **자기 브랜드 색이 아니라 백엔드 벤더의 브랜드 색**을 입고 있다. 포트폴리오 심사자가 Supabase를 안다면 즉시 알아본다.
