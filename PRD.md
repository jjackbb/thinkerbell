# [Product Requirement Document] 니편내편

## 1. Document Info
- **Service Name:** 니편내편 (NipyeonNaepyeon)
- **Author:** 팀 띵커벨
- **Document History:**
  - 2026-07-23 | 최초 작성 | 팀 띵커벨 | MVP PRD 설계도 최초 작성
  - 2026-08-20 | 패치 노트 신설 | 팀 띵커벨 | 구현 중 변경사항을 §7에 누적 기록하기로 함

> 구현하면서 바뀐 내용은 **§7 패치 노트**에 최신순으로 쌓입니다. 코드가 지금 어떤
> 상태인지 궁금하면 거기부터 보세요.

---

## 2. Executive Summary (서비스 개요)
- **One-line Definition:** 현실 관계 부담 없이 억눌린 감정을 표출하고 1초 투표 및 AI 대화를 통해 즉각적인 편들기/공감을 얻는 100% 익명 감정 배출 커뮤니티
- **Core Problem:** 현실 관계의 비난이나 평판 훼손 두려움 때문에 연애/직장 등에서 발생한 부정적 감정을 타인에게 온전히 털어놓지 못하고 솔직한 편들기/공감을 받기 어려움.
- **Value Proposition:** 완전한 익명성 보장, 1초 만에 참여하는 직관적인 '니편 vs 내편' 흑백논리 투표 여론 형성, 그리고 AI 페르소나 시뮬레이션을 통한 안전하고 시원한 감정 해소 경험 제공.

---

## 3. Target User & Persona (핵심 사용자)
- **Primary Persona:** 김지훈 / 만 29세 / 직장인(영업 직무), 1인 가구
- **Core Needs & Pain Points:** 사람들에게 부정적 감정을 털어놓자니 대인관계 악화나 평판 훼손이 걱정되고, 내 편이 되어줄 무조건적인 동의와 공감이 절실하지만 주변에 마땅한 대상이 없음.
- **User Scenario & Goal:** 퇴근 후 억울한 감정 발생 시 '니편내편' 앱 진입 ➔ 익명 사연 등록 & AI 챗봇 대화 ➔ 유저들의 '내편' 투표 및 위로 댓글 확인 ➔ 감정적 앙금 해소 및 도파민/위로 충전 후 수면.

---

## 4. MVP Feature List (MVP 기능 목록)
- **FEAT-01 | 간편 소셜 로그인 / 익명 닉네임:** 개인정보 보호 및 간편 인증을 지원하고 익명 닉네임 자동 추천/설정 | Must
- **FEAT-02 | 익명 사연 작성기:** 완전한 익명성 기반으로 제목, 카테고리, 본문을 입력하여 감정을 표출 및 사연 등록 | Must
- **FEAT-03 | 실시간 사연 목록 피드:** 최신순/투표순 사연을 카드 형태로 빠르게 스캔 및 무한 스크롤 조회 | Must
- **FEAT-04 | 사연 카테고리 필터:** 연애, 직장, 친구, 가족, 기타 등 주제별 사연 선택 필터링 | Must
- **FEAT-05 | '니편 vs 내편' 1초 투표:** A안(내편) / B안(상대편) 선택을 통한 직관적 흑백논리 여론 참여 | Must
- **FEAT-06 | 투표 결과 비율 그래프:** 투표 참여 후 실시간 지지율(%) 및 게이지 바 애니메이션 노출 | Must
- **FEAT-07 | 익명 공감 댓글 작성:** 사연 작성자에게 상세한 조언과 정서적 위로 메시지 작성 및 고유 익명 ID 부여 | Must
- **FEAT-08 | AI 1:1 시뮬레이션/대화 챗봇:** 갈등 상대 페르소나 설정 후 AI와 1:1 대화를 나눠 감정을 직접 해소 | Must
- **FEAT-09 | 비방/욕설 신고:** 부적절한 사연/댓글 신고 처리 및 5회 누적 시 자동 블라인드 | Must
- **FEAT-10 | 마이페이지 / 내 사연 및 댓글 관리:** 내가 작성한 사연, 내가 참여한 투표/댓글 리스트 조회 및 닉네임 관리 | Should

---

## 5. Functional Specifications & Policy (상세 동작 및 정책)

### [FEAT-01] 간편 소셜 로그인 / 익명 닉네임
- **상세 동작:** 1. 최초 실행 시 소셜 로그인 버튼 터치 ➔ 2. 인증 완료 후 익명 닉네임 설정창 이동 ➔ 3. 키워드 랜덤 조합 추천 제안 혹은 직접 수정 ➔ 4. 중복 검사 완료 후 웰컴 팝업과 함께 가입 완료
- **입력:** 소셜 로그인 정보(Kakao/Apple 등), 익명 닉네임 (국문/영문/숫자 2~10자)
- **출력:** 추천 닉네임 텍스트, 중복 유효성 결과 안내 메시지, 가입 완료 웰컴 모달 및 메인 피드 진입

### [FEAT-02] 익명 사연 작성기
- **상세 동작:** 1. 피드 내 '글쓰기' 버튼 선택 ➔ 2. 제목 입력 및 카테고리 선택 ➔ 3. 본문 작성 완료 후 등록 버튼 터치 ➔ 4. 등록 완료 토스트 노출 후 내 사연 상세 페이지 이동
- **입력:** 제목 (최대 30자, 필수), 카테고리 (필수), 본문 (최소 20자 ~ 최대 1,000자, 필수)
- **출력:** 등록 완료 토스트 메시지 및 등록된 사연 상세 페이지 화면

### [FEAT-03] 실시간 사연 목록 피드
- **상세 동작:** 1. 메인 탭 진입 시 실시간 최신순 피드 노출 ➔ 2. 하단 스크롤 탐색 또는 Pull-to-refresh로 실시간 최신 정보 갱신
- **입력:** 정렬 옵션 선택 (최신순 / 투표순, 선택), 스크롤 조작 (선택)
- **출력:** 카드형 사연 리스트 (제목, 본문 일부, 투표 요약, 카테고리 태그)

### [FEAT-05] '니편 vs 내편' 1초 투표
- **상세 동작:** 1. 사연 상세 본문 하단 투표 영역 진입 ➔ 2. 'A안(내편)' 또는 'B안(상대편)' 버튼 터치 ➔ 3. 1초 내 즉각 반영 및 투표 완료 상태 그래프 화면 전환
- **입력:** 투표 항목 터치 선택 (A안 / B안, 필수)
- **출력:** 투표 완료 애니메이션 피드백 및 결과 비율 그래프 노출

### [FEAT-07] 익명 공감 댓글 작성
- **상세 동작:** 1. 상세 페이지 하단 댓글 입력창 터치 후 내용 입력 ➔ 2. 등록 버튼 터치 ➔ 3. 고유 임시 익명 ID(익명 1, 익명 2 등)와 함께 리스트 상 즉시 등록
- **입력:** 댓글 본문 텍스트 (최대 200자, 필수)
- **출력:** 댓글 리스트 갱신 및 댓글 수 카운트 증가

### [FEAT-08] AI 1:1 시뮬레이션/대화 챗봇
- **상세 동작:** 1. AI 시뮬레이션 탭 진입 및 대화 상대 페르소나 선택 ➔ 2. 메시지 입력 후 전송 ➔ 3. AI 캐릭터가 지정 페르소나로 실시간 답변 반환
- **입력:** 대화 상대 페르소나 선택 (필수), 대화 메시지 입력 (최대 150자, 필수)
- **출력:** 1:1 채팅 화면 레이아웃 및 AI 말풍선 답변

### [FEAT-09] 비방/욕설 신고
- **상세 동작:** 1. 부적절한 사연/댓글 우측 더보기 메뉴 터치 후 '신고하기' 선택 ➔ 2. 신고 사유 항목 체크 및 제출 완료
- **입력:** 신고 사유 선택 (욕설/음란/비방 등, 필수)
- **출력:** 신고 접수 완료 토스트 노출 및 내부 DB 신고 누적 카운트 적재 (5회 이상 시 자동 블라인드)

---

## 6. Information Architecture (정보 구조도)
- **Depth 1: 사연 피드 / 홈 탭**
  - 메인 피드 (최신순/투표순, 주간 랭킹 top3, 오늘의 핫이슈, 밸런스 게임, 카테고리 필터)
  - 사연 상세 페이지 (사연 본문 & 1초 투표 A/B, 비율 그래프, 익명 댓글 리스트 & 작성, 신고 모달, '지금 AI와 대화하기' 연동)
  - 사연 작성 화면 (제목, 카테고리, 사연 본문 입력, AI 페르소나 자동 생성 옵션)
- **Depth 1: AI 시뮬레이션 탭**
  - AI 페르소나 선택 메인 (직장 상사, 애인, 친구, 시월드, 꼰대 선배, 사연 기반 커스텀 상대)
  - 1:1 대화 채팅방 (실시간 말풍선, 메시지 전송, 세션 종료 및 감정 해소 완료 팝업)
    - *내편지수 연출은 2026-08-20 구현에서 제외됨 — §7 PATCH-004 참고*
- **Depth 1: 마이페이지 탭**
  - 익명 프로필 / 닉네임 변경
  - 내가 쓴 사연 목록
  - 내가 참여한 투표 / 댓글 목록

---

## 7. 패치 노트 (Patch Log)

구현하면서 실제로 바뀐 것들을 **최신순**으로 기록한다. 커밋할 때마다 여기에 한 항목씩
추가한다. PRD 본문(§1~§6)과 어긋나는 결정이 생기면 본문도 같이 고치고 여기에 남긴다.

### 한눈에 보기

⚠️ 는 그냥 두면 서비스가 잘못 동작하거나 개인정보가 새는 문제였던 것.

| # | 무엇이 바뀌었나 | 왜 |
|---|---|---|
| 038 | 9/4 유저테스트 Day 0 준비 완료 기록 | 30명 대면 테스트 전날까지의 준비 상태를 팀원이 한 파일로 볼 수 있게 |
| 037 | 예비 AI가 쓰는 모델 이름을 한 곳으로 | 새 키에서 `gemini-2.5-flash`가 막혀 예비선이 살아나지 못했음. 네 군데에 흩어져 있던 이름을 상수로 모음 |
| **036** ⚠️ | AI가 답을 못 주면 실패했다고 말한다 | **AI가 죽어도 미리 적어둔 대사를 AI가 말한 것처럼 띄웠음.** 9/4 유저테스트 데이터가 통째로 오염될 뻔함 |
| **035** ⚠️ | 5개 화면 유저플로우 전체 재점검 | 문서가 실제 화면과 어긋나 있었고, **화면이 하는 말과 다르게 동작하는 곳 6군데**를 찾음 |
| **034** ⚠️ | 로그인 없이 둘러볼 수 있는 범위를 넓힘 | 읽지도 못하게 막으니 가입할 이유를 만들 기회가 없었음 |
| **033** ⚠️ | 사연 상세 화면 유저플로우 정리 | 댓글 초안이 다른 사연으로 새고, 조회수가 DB에 안 쌓이고 있었음 |
| **032** ⚠️ | 밸런스 게임을 진짜로 동작하게 | 표수가 지어낸 값이라 투표해도 아무 데도 저장되지 않았음 |
| 031 | 가짜 투표·조회수를 전부 0으로 | 유저 테스트에서 실제 숫자가 쌓이게 하려고 |
| 030 | 투표 전 게이지의 경계를 녹여 가림 | 회색 빈 막대라 궁금해지지가 않아 투표할 이유가 없었음 |
| 029 | '이 사연의 쟁점' 기능 삭제 | 양쪽 주장을 마주 세우면 해소가 아니라 싸움이 됨 |
| **028** ⚠️ | 사연을 지우면 댓글·투표도 함께 삭제 | 사연을 지워도 남의 댓글이 DB에 계속 남아 있었음 |
| 027 | 홈 화면 군더더기 정리 + 테스트 글 삭제 | 안 눌리는데 눌러보게 생긴 것, 도달도 못 하는 화면이 있었음 |
| 026 | 문의에 답변할 수 있게 (운영자 화면) | 문의가 쌓이기만 하고 답할 방법이 없었음 |
| 025 | 패치 노트에 한눈에 보기 요약 추가 | 무엇이 바뀌었는지 훑어보기 어려웠음 |
| **024** ⚠️ | 마이페이지 버튼들이 실제로 동작 | 비밀번호 변경·탈퇴·문의가 눌러도 반응 없는 껍데기였음 |
| 023 | AI 대화 6개월 뒤 자동 삭제 | 가장 사적인 데이터를 무기한 쌓아두지 않기 위해 |
| 022 | AI 대화 전체 삭제 버튼 | 털어놓은 이야기를 한 번에 지울 수 있어야 함 |
| **021** ⚠️ | AI 대화를 서버에 저장 | 기기를 바꾸면 대화가 안 따라오고, 캐시 지우면 사라졌음 |
| **020** ⚠️ | 대화방을 계정별로 분리 | 같은 브라우저에서 다른 계정 로그인 시 **남의 대화가 보였음** |
| 019 | 기본 페르소나 제거 + 빈 화면 안내 | 사연과 무관한 데모 캐릭터가 핵심 흐름을 흐림 |
| 018 | 예전 페르소나 전부 정리 | 옛 이름 규칙과 새 규칙이 섞여 목록이 뒤죽박죽 |
| 017 | AI 대화 화면 높이 수정 | 입력창이 하단 메뉴에 가려 타자를 치려면 스크롤해야 했음 |
| 016 | 사연 쓰기 버튼을 피드에만 표시 | AI 탭에서 '시작하기' 버튼을 덮고 있었음 |
| 015 | 아이콘 버튼에 이름 부여 | 스크린리더가 "버튼"이라고만 읽음 |
| **014** ⚠️ | 공유 링크 진입 시 투표 상태 복원 | 이미 투표했는데 "투표하세요"가 뜨고 공유 바가 안 나옴 |
| 013 | 공유 카드 퍼센트 대비 수정 | 비율이 작으면 숫자가 반대편 색 위에 얹혀 안 읽힘 |
| 012 | 신고 모달에 사유 선택 추가 | "사유를 선택하세요"라면서 고를 것이 없었음 |
| **011** ⚠️ | 댓글 공감이 서버에 저장 | 화면에서만 오르고 DB에 안 남았음 (008과 같은 원인) |
| 010 | 파비콘·메타 태그 | 매 접속마다 콘솔 404, 탭 아이콘 비어 있었음 |
| 009 | 무료 횟수 조회 방식 변경 | 콘솔에 404/ERR_ABORTED가 계속 찍힘 |
| **008** ⚠️ | 투표가 서버에 저장 | **화면만 바뀌고 DB에 한 건도 안 남았음.** 새로고침하면 사라짐 |
| 007 | 공감 모드 발신자 이름 수정 | 사연 제목이 발신자로 찍혀 갈등 상대가 위로하는 것처럼 보임 |
| 006 | 조사(와/과) 교정 | "상대방와의 대화"가 그대로 노출 |
| 005 | 패치 노트 규칙 문서화 | 팀원이 변경 내역을 이 파일 하나로 파악하도록 |
| 004 | 내편지수 제거 | 공감 비율을 숫자로 베낀 값일 뿐이고 화면에 안 쓰임 |
| **003** ⚠️ | 무료 횟수를 서버에서 카운트 | 브라우저 저장소만 지우면 무제한이었음 |
| 002 | 대화 종결 기호 상수화 | 양쪽에 하드코딩돼 한쪽만 고치면 대화가 안 끝남 |
| 001 | AI 프롬프트 통합·난이도 상향 | 프롬프트가 두 벌로 갈라져 AI가 다르게 굴었음 |

### 쓰는 방법

각 항목은 **한 줄 요약**으로 시작한다. 자세한 내용은 그 아래에 적는다.

```
### PATCH-0NN | YYYY-MM-DD | 제목

> **요약:** 한 문장. 무엇이 어떻게 바뀌었는지.

- **증상/무엇을:** ...
- **왜:** ...
- **확인:** 어떻게 검증했는지
- **파일:** 손댄 파일
```

---

### PATCH-038 | 2026-09-02 | 9/4 유저테스트 Day 0 준비를 끝내고 기록했다

> **요약:** 9/4에 새싹학교 30명을 대상으로 대면 유저테스트를 한다. 그 전에 "30명이
> 실제로 들어올 수 있는 상태"를 만드는 준비 4단계를 오늘 전부 끝냈다.

- **무엇을:**
  - **완주 점검** — 저장소를 비운 첫 사용자 상태로 진입→가입→피드→상세→투표→AI 3턴을
    실제 화면으로 한 바퀴 돌았다. 이메일 확인이 실제로 꺼져 있음을 확인했고, 막히거나
    걸리는 지점 9건을 목록으로 남겼다.
  - **테스트 계정 30개** — `test01`~`test30` 발급, 전부 인증 완료. 인쇄용 계정표와 현장
    운영 규칙을 따로 만들었다.
  - **AI 상태 점검** — 주력(Potens) 정상·한도 여유(사용량 1%), 예비(Gemini) 복구,
    응답 속도 실측(주력 4~6.5초 / 예비 1.5초).
- **왜:** 결함 수정보다 이게 먼저다. 접속이 안 되거나 로그인이 막히면 그날이 통째로
  날아가지만, 결함 몇 개는 남아 있어도 테스트는 성립한다.
- **새로 찾은 것:** **로그인해도 피드 화면에는 로그인했다는 표시가 하나도 없다.**
  헤더 오른쪽 버튼을 감추는 조건(`activeTab !== 'feed'`)이 로그인한 사람에게까지 걸려
  있다. 30명의 첫 질문이 "이거 로그인 된 거예요?"가 된다 — 내일 수정 목록에 넣었다.
- **확인:** 아이폰 크기(390×844) 화면 스크린샷, `auth.users` 조회, 프로덕션 API 직접 호출.
- **파일:** `docs/usertest-20260904.md`, `docs/usertest-accounts.md`

---

### PATCH-037 | 2026-09-02 | 예비 AI가 쓰는 모델 이름을 한 곳으로 모았다

> **요약:** 예비 AI(Gemini)에 새 키를 넣었더니 이번엔 모델 이름이 낡아서 막혔다.
> 네 군데에 박혀 있던 모델 이름을 한 곳으로 모으고 현행 모델로 바꿨다.

- **증상:** 새로 발급한 Gemini 키를 넣고 예비선을 확인하니 구글이 404를 돌려줬다 —
  `"models/gemini-2.5-flash is no longer available to new users"`. 키는 정상인데
  **모델이 신규 사용자에게 닫혀 있었다.**
- **왜:** 같은 모델 이름이 네 군데(대화 2곳, 사연 검사 2곳)에 각각 박혀 있어서, 한 군데를
  고쳐도 나머지가 조용히 죽어 있게 된다. 9/4 유저테스트에서 주력(Potens)이 멈추면
  기댈 곳이 이 예비선뿐이다.
- **무엇을:** `GEMINI_MODEL` 상수 하나로 모으고 기본값을 `gemini-3.5-flash-lite`로 잡았다.
  배포 환경변수 `GEMINI_MODEL`로 덮어쓸 수 있다.
- **정책:** 예비선의 임무는 최고 품질이 아니라 **주력이 죽은 순간 대화를 끊기지 않게 하는
  것**이다. 그래서 싸고 빠른 Flash-Lite를 기본값으로 뒀다. 최악의 경우(30명이 전부
  Gemini로 넘어가 450건) 예상 비용은 500원 안팎이고, Potens가 살아 있으면 0원이다.
- **확인:** 배포 후 주력을 일부러 실패시켜 예비선으로 떨어뜨리고, Gemini가 실제로
  한국어 답을 돌려주는 것을 확인했다.
- **파일:** `api/index.ts`

---

### PATCH-036 | 2026-09-02 | AI가 답을 못 주면 실패했다고 말한다

> **요약:** AI가 멈췄을 때 미리 적어둔 대사를 AI가 말한 것처럼 보여주던 것을 없애고,
> "답장을 받지 못했어요"와 **다시 보내기** 버튼을 띄우도록 바꿨다.

- **증상:** AI 호출이 실패해도 화면은 아무 일 없다는 듯 페르소나의 대사를 하나 띄웠다.
  세 군데에 그런 길이 있었다 — ① 요청이 터졌을 때(`"그 논리가 정말 맞다고 생각하시나요?"`),
  ② 한 글자도 못 받았을 때(`"논리적으로 다시 설명해 보세요."`), ③ 서버에 쓸 수 있는 AI가
  하나도 없을 때(`"너 정말 너무하다..."` 를 한 글자씩 흘려보냄).
- **왜:** 9/4에 30명 대면 유저테스트를 한다. AI 대화가 이번 테스트의 핵심 측정 대상인데,
  AI가 죽은 채로 가짜 대사가 흐르면 **테스터는 "AI가 이상하다"고만 느끼고 로그에는
  정상 대화(3턴 도달)로 쌓인다.** 무엇이 고장났는지 그날 밤 분석에서 알 길이 없다.
  같은 점검에서 **예비 AI(Gemini)의 프로덕션 키가 무효**라는 것도 확인했다. 주력(Potens)이
  멈추면 곧바로 이 경로로 떨어진다.
- **영향:** 실패한 내 말은 화면에 남고, 바로 아래 `다시 보내기`로 그대로 재전송된다.
  실패 안내 문구는 AI에게 보내는 대화 기록에서 제외해 다음 답변을 흐리지 않는다.
- **확인:** AI 키를 전부 뺀 서버로 실제 대화를 열어 메시지를 보냈다. 가짜 대사 대신
  실패 안내와 `다시 보내기`가 떴다(스크린샷). 키를 되돌린 뒤 같은 화면에서 보내니
  AI가 정상적으로 답했다. `tsc --noEmit` 통과.
- **파일:** `src/components/AIChatView.tsx`, `api/index.ts`

---

### PATCH-035 | 2026-08-26 | 5개 화면 유저플로우 전체 재점검

> **요약:** 감사 문서가 실제 화면과 어긋나 있어 전부 다시 확인했다. 그 과정에서
> **"됐다"고 알려놓고 실제로는 아무 일도 안 일어나는 곳 6군데**를 찾았다.

- **왜 다시 했나:** 문서의 1번 화면 표가 헤더 버튼 4개를 있는 것처럼 적어두고 있었는데,
  지금 화면에는 로고 하나뿐이다. 코드를 보고 쓴 표가 화면과 어긋나 있었다.
  나머지 화면 3개는 아예 미착수 상태였다.

- **무엇을:** 브라우저로 실제 화면을 띄우고 운영 DB를 조회해 5개 화면을 전부 훑었다.
  `docs/userflow.md`에 화면별 요소 표와 결함을 다시 썼다. 맨 위에 '한눈에 보기' 표를
  넣어 결함 21건을 한 화면에서 보게 했다.

- **찾은 것 (⚠️ = 화면이 하는 말과 실제 동작이 다름):**

  | | 결함 |
  |---|---|
  | ⚠️ | `숨기기`를 눌러도 저장되지 않는데 "숨겼습니다"라고 알린다. **사연 1건(`진짜 이사님 죽통 꽂고 싶어요`)은 이미 아무에게도 안 보인다** |
  | ⚠️ | 닉네임을 바꿔도 계정에 저장되지 않는다 (성공 메시지는 뜬다) |
  | ⚠️ | 알림 설정 3종이 아무 알림도 보내지 않는다 — 발송 경로 자체가 없다 |
  | ⚠️ | AI 오류 신고가 `console.log`만 찍고 저장되지 않는다 |
  | ⚠️ | AI 대화 종료 요약의 기분·한 줄 소감이 저장되지 않고, `여기서 마무리`가 경고 없이 대화를 영구 삭제한다 |
  | ⚠️ | 이의 제기가 접수만 되고, 상태를 바꿀 코드도 볼 화면도 없다 |
  | | 주간 랭킹 배너가 통째로 안 보인다 (실시간 랭킹까지 같이 죽음) |
  | | `HOT`이 `최신순`과 결과가 완전히 같다 |
  | | 둘러보는 사람이 신고 모달을 끝까지 열고, 제출하면 영문 오류를 본다 |
  | | 모달 13개에 `role="dialog"`·포커스 가둠·ESC가 하나도 없다 |
  | | 그 외 11건 (마이페이지 내역 미클릭, 탈퇴 사유 유실 등) |

- **확인:** 브라우저에서 둘러보기 상태로 피드·사연 상세·AI 탭·마이 탭을 띄워
  화면을 캡처했다. `숨기기` 결함은 **실제로 눌러본 뒤 운영 DB를 조회해** 화면은
  사라졌는데 `isHidden`이 `false` 그대로인 것을 확인했다. 알림 발송 경로 부재는
  Edge Functions·`cron.job` 목록을 원격 조회해 확인했다.

- **한계:** 로그인해야 보이는 화면(마이페이지 6화면, AI 대화방)은 **아직 실제로
  띄워 보지 못했다.** 코드와 DB로만 확인했다. 테스트 계정이 생기면 화면으로
  다시 검증한다.

- **아직 안 고쳤다.** 이번 커밋은 문서만 바꾼다. 무엇부터 고칠지 정하고 나서 손댄다.

- **파일:** `docs/userflow.md`, `PRD.md`

---

### PATCH-034 | 2026-08-26 | 로그인 없이 둘러볼 수 있는 범위를 넓힘

> **요약:** 둘러보기로 들어오면 사연 목록만 보이고 눌러볼 수 있는 게 거의 없었다.
> 이제 사연 본문과 댓글을 읽고, AI 상대가 먼저 말을 거는 것까지 볼 수 있다.

- **무엇을:**

  | 곳 | 예전 | 지금 |
  |---|---|---|
  | 사연 상세 | 열리지 않음 | 본문·댓글 읽기 |
  | AI 시뮬레이션 | 도달 불가 | 상대가 먼저 거는 첫 마디까지 |
  | AI 탭 | 탭 진입 자체가 차단 | 진입 가능 — "대화는 사연에서 시작해요" 안내 |
  | 마이 탭 | 탭 진입 자체가 차단 | 로그인 안내 + 자주 묻는 질문 |
  | 헤더 이름 | `속뚫리는고구마` | `둘러보는 중` |

  막힌 채로 두는 것: 투표, 댓글 쓰기, 공감, 신고, 사연 등록.

- **왜:** 읽지도 못하게 막으면 이 서비스가 무엇인지 알 방법이 없어 **가입할 이유가
  안 생긴다.** 편을 들거나 말을 얹는 것만 로그인을 요구하면 충분하다.
  투표를 안 했으니 비율은 자연히 봉인된 채로 보인다 — 읽을 이유와 가입할 이유가
  같은 화면에 함께 있게 된다.

- **정책 — 못 쓰는 건 처음부터 못 쓰게 보인다:** 댓글 입력창과 AI 입력창을
  게스트에게는 **아예 그리지 않는다.** 열어두면 다 써놓고 보낼 때 막히는데,
  그때는 이미 쓴 글을 잃는다. 그 자리에 로그인 안내와 버튼을 둔다.
  반대로 투표 버튼은 한 번의 클릭이라 눌렀을 때 안내하는 것으로 충분하다.

- **⚠️ 함께 고친 것 — 둘러보기가 새로고침을 못 견뎠다:** 게스트 여부가 화면
  상태로만 있어서 **새로고침하면 '로그인한 사람'으로 되돌아갔다.** 사연 상세가
  막혀 있을 때는 티가 안 났지만, 이제 둘러보는 사람이 그 화면에 머문다 —
  그 상태로 새로고침하면 댓글창이 열린 것처럼 보이고, 써서 등록하면 서버가
  조용히 거절한다. 브라우저에 기억시켜 새로고침을 견디게 했다.

- **함께 고친 것 — 가짜 닉네임:** 게스트도 `속뚫리는고구마` / `카카오 가입`이라는
  임시 정보를 들고 다녔다. 가입하지도 않은 사람에게 남의 이름표를 달아주는
  꼴이라 헤더에서 `둘러보는 중`으로 바꿨고, 마이 탭 안내에도 쓰지 않는다.

- **함께 고친 것 — 무료 횟수:** 게스트는 AI 무료 횟수를 세지 않는다. 어차피
  답장을 못 하므로 쓴 것이 없는데, 세면 답장도 못 하는 사람에게 결제 안내가 떴다.

- **확인:** 브라우저에서 둘러보기로 들어가 전 경로를 밟았다 — 사연 상세 열림,
  조회수 0→1 증가, 봉인 게이지 유지, AI가 "어 왔어? 별일 없지?"로 먼저 말 걸고
  입력창 자리에 로그인 안내, 마이 탭 안내 + FAQ, 새로고침 후에도 게스트 유지,
  모바일 375px 정상, 가로 스크롤 없음, 콘솔 오류 0. 타입체크 통과.

- **⚠️ 뒤이어 고친 것 — 새로고침할 때마다 로그인 창이 막아섰다:** 세션이 없으면
  무조건 환영 모달을 띄우고 있었다. 둘러보는 사람에게도 예외가 없어서, 새로고침을
  할 때마다 로그인 창이 화면을 가로막았다. **둘러보기 자체가 성립하지 않았다.**
  게스트 상태를 기억하게 만들면서 드러난 문제다.

- **헤더:** 둘러보는 사람에게는 오른쪽 버튼(`사연 등록`·이름)을 감춘다.
  사연 등록은 눌러봐야 로그인 안내로 막히고, 이름 버튼은 지금 있는 마이 탭으로
  다시 보내는 것뿐이라 둘 다 헛걸음이다.

- **파일:** `src/App.tsx`, `src/components/AIChatView.tsx`,
  `src/components/MyPageView.tsx`, `src/components/StoryDetailModal.tsx`,
  `src/components/Header.tsx`, `PRD.md`

### PATCH-033 | 2026-08-26 | 사연 상세 화면 유저플로우 정리

> **요약:** 사연 상세에서 눌러도 아무 일 없던 것들을 고쳤고, 댓글을 쓰다 만 글이
> 다른 사연으로 따라가던 문제와 조회수가 실제로는 하나도 안 쌓이던 문제를 잡았다.

- **무엇을:**

  | 무엇이 | 예전 | 지금 |
  |---|---|---|
  | 모달 상단 로고 | 눌릴 것처럼 생겼는데 아무 일 없음 | `사연` 제목으로 교체 |
  | ESC 키 | 아무 일 없음 | 안쪽부터 4단계로 닫힘 (글 쓰는 중엔 무시) |
  | `12 Views` / `3 Comments` | 여기만 영어 | `조회 12` / `댓글 3` |
  | 공유 바 '미리보기' | 항상 눌리지 않는 회색 글자 | 실제 카드 이미지를 그려 보여줌 |
  | 댓글 버튼 | `입력` | `등록` |
  | 마이페이지에서 사연 열기 | 다른 길로 열려 조회수·주소가 빠짐 | 피드와 같은 길로 통일 |

- **⚠️ 댓글 초안이 다른 사연으로 새어 나갔다:** 모달은 사연이 바뀌어도 다시 만들어지지
  않는데 입력창 내용을 비우지 않았다. **A 사연에 쓰다 만 댓글이 B 사연 입력창에 그대로
  따라갔다.** 익명 서비스에서 엉뚱한 사연에 등록되면 되돌릴 수 없다.

- **⚠️ 조회수가 DB에 전혀 쌓이지 않았다:** `supabase.rpc(...)`에 `.then()`이 없어
  **네트워크 요청 자체가 나가지 않았다.** 화면 숫자만 로컬로 오르고 DB는 그대로였다.
  조회수는 신고 비율 판정의 분모라, 그만큼 분모가 실제보다 작았다.

- **정책:** 댓글의 '전체 / 니 편 / 내 편' 진영 필터는 **남긴다.** 한때 해소 원칙에
  어긋난다고 봤으나, 사람을 진영별로 *묶어서 대립시키는* 장치와 반대편을 *이해하러
  건너가는* 통로는 다르다. 형태만 보고 원칙을 기계적으로 적용하지 않는다.

- **함께 정리:** 봉인 게이지 규칙이 사연용·배너용으로 두 벌 존재하던 것을
  `VoteResult` 한 곳으로 합쳤다(`labels`, `surface` prop 추가). 색이 전부
  `index.css` 변수로 빠져 **컴포넌트 안에 hex가 하나도 없다.**

- **확인:** 타입체크 통과. 별도 검증 에이전트가 브라우저로 18개 항목을 실측했다 —
  ESC 4단계, 미리보기 오버레이 위치(모달 카드와 좌표 일치), 클릭→렌더 71ms,
  마이페이지에서 열고 닫아도 탭이 안 튐, 모바일 375px, 가로 스크롤 없음, 콘솔 오류 0.
  거기서 나온 결함 3건을 고친 뒤 다시 확인했다.

- **파일:** `src/components/StoryDetailModal.tsx`, `src/components/VoteResult.tsx`,
  `src/App.tsx`, `src/index.css`, `docs/userflow.md`, `PRD.md`

### PATCH-032 | 2026-08-26 | 밸런스 게임을 진짜로 동작하게

> **요약:** 홈 상단 밸런스 게임의 표수는 코드에 적어둔 지어낸 값이었고, 투표해도
> 새로고침하면 사라졌다. 이제 진짜로 저장되고 0부터 쌓인다.

- **무엇을:**

  | | 예전 | 지금 |
  |---|---|---|
  | 표수 | 하드코딩 (782·450·820) | DB에 실제 저장, **0부터** |
  | 저장 | 컴포넌트 상태만 +1 | `balance_votes` 테이블 |
  | 중복 방지 | 없음 | 한 사람당 한 게임 한 표, 변경 1회 |
  | 투표 전 비율 | **78% / 22% 그대로 노출** | 봉인 게이지 (사연과 같은 규칙) |
  | 비로그인 클릭 | 그냥 눌림 (저장 안 됨) | 로그인 안내 |
  | 반응 | 10초 뒤 확정 | 즉시 |

- **왜:** 사연 카드는 투표 전 비율을 가리는데 **바로 위 배너는 다 보여주고 있어**
  서로 모순됐다. 게다가 그 숫자는 아무 근거가 없었다. 포트폴리오에서 지어낸 숫자는
  치명적이다.

- **10초 지연을 없앤 이유:** 누른 뒤 10초 동안 아무 변화가 없으면 눌렸는지 알 수 없어
  한 번 더 누르게 된다. 이 감사에서 없애려던 **무반응** 그 자체였다.

- **정책:** 투표 버튼의 좌우 순서를 바꿨다. 앱 전체가 왼쪽 = 니 편(인디고),
  오른쪽 = 내 편(코랄) 규칙이라, 게이지를 공유하면 버튼도 맞춰야 "왼쪽 버튼 색 =
  왼쪽 막대 색"이 된다. 감정을 대변하는 선택지가 '내 편' 코랄에 앉는 쪽이 색 언어와도
  맞는다. 세 가지 안을 시안으로 비교해 골랐다.

- **보안:** `balance_votes`는 RLS를 켜고 기존 `votes`와 같은 정책(본인 것만 조회)을
  줬다. 쓰기 정책은 일부러 두지 않았다 — 클라이언트는 `vote_balance_game()` 함수로만
  투표할 수 있고, 이 함수는 `anon`에게서 실행 권한을 뺏었다.

- **한계:** 질문 3개는 여전히 코드에 있다. 질문을 바꾸려면 배포해야 한다. 지금 필요한
  건 표를 쌓는 것뿐이라 테이블로 옮기지 않았다.

- **확인:** 게스트로 눌러 로그인 안내가 뜨는 것, 로그인 후 투표가 DB에 실제로 들어가고
  새로고침 후에도 남는 것, 변경 1회 제한이 거부되는 것, 검은 배경에서 게이지 대비비가
  2.4~2.8:1로 읽히는 것을 브라우저에서 확인했다.

- **파일:** `src/components/BalanceGameSection.tsx`, `src/components/VoteResult.tsx`,
  `src/index.css`, `src/App.tsx`, 마이그레이션 `balance_game_votes`, `docs/userflow.md`, `PRD.md`

### PATCH-031 | 2026-08-26 | 가짜 투표·조회수를 전부 0으로

> **요약:** 시드로 넣어둔 사연에 붙어 있던 투표수(43~293표)와 조회수(89~890)를
> 전부 0으로 내렸다. 유저 테스트에서 진짜 숫자가 쌓이게 하려고.

- **무엇을:** `stories`의 `votesA`·`votesB`·`viewCount`를 0으로, `votes` 테이블을 비웠다.
- **왜:** 아무도 안 봤는데 890회로 표시되고 있었다. 유저 테스트 때 실제 반응을 보려면
  출발점이 0이어야 한다.
- **`votes` 행까지 지운 이유:** 행이 남아 있으면 그 계정은 같은 사연에 재투표를 못 해
  테스트가 막힌다. 이 때문에 시드 5건뿐 아니라 실제로 쓴 사연 3건의 표도 함께 지웠다.
- **남긴 것:** 댓글 21개. 표시값과 실제 행 수가 정확히 일치하는 진짜 댓글이라 건드리지 않았다.
- **되돌리기:** 지우기 전 값을 scratchpad에 백업해뒀다.
- **확인:** 조회수 합 0, 표 합 0, 사연 8건·댓글 21개 유지를 SQL로 확인했다.
- **파일:** (DB만) `PRD.md`

### PATCH-030 | 2026-08-25 | 투표 전 게이지의 경계를 녹여서 가림

> **요약:** 투표하기 전 게이지가 그냥 회색 빈 막대여서 결과가 궁금해지지 않았다.
> 이제 진짜 비율 막대를 옅은 색으로 깔되 경계선 자리를 넓게 녹여, 대충 어느 쪽인지는
> 느껴지되 정확히 어디서 갈리는지는 못 짚게 했다.

- **무엇을:** 사연 카드·상세의 투표 전 게이지를 바꿨다.

  | | 예전 | 지금 |
  |---|---|---|
  | 막대 | 회색 빈 막대 | 진짜 비율 막대(옅은 진영색), 경계를 막대 폭의 **35%**에 걸쳐 녹임 |
  | 라벨 | 니 편 / 내 편 | 니 편 **??%** / **??%** 내 편 |
  | 문구 | "투표하면 결과가 열려요" | "벌써 **N명**이 편을 골랐어요" |
  | 아무도 안 골랐을 때 | (같은 문구) | "아직 아무도 안 골랐어요 · **첫 번째가 되어보세요**" |

- **왜:** 결과를 완전히 감추는 것 자체는 안전했지만, 회색 빈 막대는 "가려놨다"가 아니라
  "아무것도 없다"로 읽혔다. 열어보고 싶은 마음이 안 생기니 투표할 이유도 없었다.

- **정책:** 목표는 완전 차단이 아니라 **적당히 보이면서 궁금해지는 것**이다.
  전이 폭을 여러 단계로 그려 비교한 결과 35%를 골랐다. 10~20%는 경계 중심점이
  그대로 짚혀 대략적인 숫자까지 읽히고, 50%는 70:30과 85:15가 거의 같아 보여
  사연끼리의 차이가 사라진다. 35%가 사연이 몰리는 55~70 구간에서 서로 다른 인상을
  주면서도 경계를 못 짚는 유일한 폭이었다.

- **한계:** 표가 한쪽으로 크게 쏠린 사연(85:15 등)은 경계가 막대 끝에 붙어 전이가
  잘리므로 쏠렸다는 사실이 드러난다. 막을 방법이 없고, 지금 목표상 막을 이유도 없다.

- **한계(색):** 옅은 색은 `--color-your-side`·`--color-my-side` 토큰을 흰색에 55%
  섞어 만들었다. 진영색 자체가 흰 배경에서 대비가 약한 문제(`#FF6B5A` 2.8:1로
  UI 최소 기준 3:1 미달)가 따로 있는데, 이 색이 소스에 **215곳 하드코딩**돼 있어
  유저플로우 감사가 끝난 뒤 토큰화와 함께 한 번에 손대기로 했다.

- **함께 발견:** 상단 밸런스 게임 배너는 투표 전인데도 비율(78%/22%)을 그대로 보여준다.
  게다가 그 표수(782·450·820)는 **하드코딩된 가짜 값**이라 DB에 저장되지도 않는다.
  별도 항목으로 남긴다.

- **확인:** 타입체크 통과. 실제 화면(localhost:3000)에서 표 3명·5명·100명짜리 사연이
  모두 녹은 경계 + "벌써 N명이 편을 골랐어요"로 나오는 것을 스크린샷으로 확인했다.
  콘솔 오류·경고 0.

- **파일:** `src/components/VoteResult.tsx`, `src/index.css`, `PRD.md`

### PATCH-029 | 2026-08-25 | '이 사연의 쟁점' 기능 삭제

> **요약:** 사연을 '한쪽 주장 vs 다른 쪽 주장'으로 요약해 나란히 보여주는 카드가 있었다.
> 털어놓으러 온 사람을 이기러 오게 만드는 장치라 통째로 걷어냈다.

- **무엇을:** `IssueSummary` 컴포넌트를 지우고, 사연 카드·상세에서 걷어냈다.
  사연 데이터에서 `issueMySide`·`issueYourSide` 두 항목을 없애고, DB 컬럼도 지웠다.

- **왜:** 그 카드는 애초에 **화면에 한 번도 제대로 뜬 적이 없었다** — 값을 채워 넣는
  입력칸이 어디에도 없었다. 살릴지 지울지 판단하려고 AI로 값을 채운 시안을 만들어
  봤는데, 보고 나서 결론이 명확해졌다. 양쪽 주장을 대칭으로 세워 "어느 쪽인가요?"를
  물으면 사연이 *반박 가능한 명제*가 된다. 그 순간 서비스는 해소가 아니라 논쟁이 된다.

- **정책:** **니편내편의 목적은 해소지 논쟁이 아니다.** PRD 본문에는 이 기능이
  '이탈 방지 장치'로 정당화돼 있었지만, 이 원칙이 그보다 우선한다. 앞으로 양쪽 주장을
  나란히 세우거나 진영별로 사람을 묶는 장치는 같은 이유로 넣지 않는다.
  같은 잣대로 재검토할 것: 댓글의 '전체 / 니 편 / 내 편' 진영 필터.

- **확인:** 타입체크 통과. 실제 화면에서 카드가 사라진 것과 콘솔 오류 0을 확인했다.

- **파일:** `src/components/IssueSummary.tsx`(삭제), `src/components/StoryCard.tsx`,
  `src/components/StoryDetailModal.tsx`, `src/components/CreateStoryModal.tsx`,
  `src/App.tsx`, `src/types.ts`, 마이그레이션 `drop_issue_summary_columns`, `PRD.md`

### PATCH-028 | 2026-08-24 | 사연을 지우면 딸린 것들도 함께 지워지게

> **요약:** 사연을 지워도 거기 달렸던 댓글과 투표가 DB에 그대로 남아 있었다.
> 이제 사연을 지우면 함께 지워진다. 단, AI 대화방은 일부러 남긴다.

- **무엇을:** `comments`·`votes`·`ai_personas`·`ai_chat_usage`의 `storyId`를 실제
  연결(외래키)로 등록했다. 테이블마다 규칙을 다르게 줬다.

  | 테이블 | 사연이 지워지면 | 왜 |
  |---|---|---|
  | 댓글 | 함께 지워진다 | 사연이 없으면 댓글도 갈 곳이 없다 |
  | 투표 | 함께 지워진다 | 마찬가지 |
  | **AI 대화방** | **남는다** (연결만 끊김) | 사연이 없어져도 내가 나눈 대화는 내 것이다 |
  | 무료 횟수 기록 | 남는다 (연결만 끊김) | "썼다"는 사실이 남아야 정산이 맞다 |

  신고(`reports`)는 사연과 댓글을 모두 가리키는 컬럼이라 연결을 걸 수 없어 그대로 뒀다.

- **왜:** 홈 화면 점검(PATCH-027) 중 테스트 사연을 지우다 발견했다. 사연은 사라졌는데
  거기 달린 댓글이 주인 없이 남았다. 원인은 `stories`를 가리키는 연결이 **하나도**
  없었다는 것이다. 지웠다고 생각한 글의 댓글이 계속 남는 건 개인정보 문제이기도 하다.

- **한계였던 것:** 앱의 삭제 코드(`confirmDeleteStory`)만 고치면 SQL로 직접 지울 때
  또 남는다. 그래서 앱이 아니라 DB에 규칙을 걸었다.

- **영향 없음:** **탈퇴는 원래 무사했다.** `delete_my_account()`가 사연·댓글·투표·신고를
  함수 안에서 직접 다 지우고 있었다. 문제는 사연 한 건 삭제 경로뿐이었다.

- **확인:** 임시 사연에 댓글·투표·AI 대화방을 달아 실제로 지워봤다. 댓글·투표는 0이 되고
  AI 대화방은 남은 채 연결만 끊겼다. 댓글 수를 세는 트리거와 충돌하지 않는 것도 확인했다.
  검사 데이터는 전부 치웠다 (사연 8건, 댓글 21건, 고아 0건).

- **파일:** 마이그레이션 `link_story_children_with_foreign_keys`, `docs/userflow.md`, `PRD.md`

### PATCH-027 | 2026-08-24 | 홈 화면 유저플로우 1차 정리

> **요약:** 홈 화면에서 눌러도 아무 일 없는 것, 같은 곳으로 두 번 가는 것, 도달조차
> 못 하는 화면을 걷어냈다. 사연 쓰기 버튼은 화면 아래에 계속 떠 있게 바꿨다.

버튼과 요소를 하나씩 놓고 "유저가 이걸 왜 누르나"를 따지는 점검을 시작했다.
홈 화면이 1차 대상이고, 점검표는 `docs/userflow.md`에 있다.

- **무엇을:**
  1. **API 키 설정 화면을 통째로 삭제.** 'Potens.ai API Key 설정' 모달이 있었는데
     여는 버튼이 어디에도 없어 도달할 수 없었다. 서버가 이미 자기 키(`.env`)로만
     부르고 있어서, 브라우저가 키를 실어 보내던 경로도 함께 사라졌다.
  2. **홈 화면 헤더에서 '사연 등록'과 닉네임 버튼을 뺐다.** 닉네임 버튼은 하단
     '마이' 탭과 완전히 같은 코드였다. 다른 탭에서는 그대로 보인다.
  3. **사연 쓰기 안내를 화면 아래 고정 바로.** 예전에는 목록 맨 아래에 한 번
     나와서, 사연 8개를 지나 끝까지 내린 사람만 봤다.
  4. **투표 전 게이지에서 색을 뺐다.** 45:55로 칠하고 흐리게만 처리해서, 작게 보면
     흐릿한 진짜 결과처럼 읽혔다. 빈 회색 막대와 자물쇠로 바꿨다.
  5. **댓글 수와 투표 수를 구분했다.** 둘 다 회색 아이콘+숫자로 똑같이 생겼는데
     댓글만 눌렸다. 댓글에 테두리를 줘 버튼으로 보이게 했다.
  6. **테스트 사연 3건 삭제** (`ㅇㅇㅇ`, `ㄴㅇㄹㅁㄹ`, 키보드 난타 1건). 사연 11 → 8건.

- **왜:** 지울 버튼에 나중에 분석 이벤트를 붙이면 데이터가 오염된다. 계측을 붙이기
  전에 화면부터 정리해야 해서 이 순서로 잡았다.

- **확인:** 타입체크 통과. 브라우저에서 홈/마이 탭 각각 스크린샷으로 대조 —
  홈에서만 헤더 버튼이 사라지고 하단 바가 뜨는 것, 마지막 카드가 바에 가리지 않는 것,
  게이지·아이콘이 구분되는 것을 확인했다. 콘솔 오류 0, 가로 스크롤 없음,
  `potens_api_key`가 localStorage에서 제거된 것까지 확인.

- **한계:** 밸런스 게임 배너의 화살표와 점이 같은 일을 하는 중복은 2차로 미뤘다.
  게스트에게도 헤더에 `속뚫리는고구마`라는 닉네임이 보이는 문제는 아직 남아 있다.

- **영향:** 사연을 지워도 댓글·투표가 남는 결함을 이번에 찾았다. `stories`를 가리키는
  외래키가 하나도 없고 `confirmDeleteStory`도 사연 행만 지운다. **탈퇴는 무사하다** —
  `delete_my_account()`가 함수 안에서 직접 다 지운다. 고치는 방법은
  `docs/userflow.md`에 적어두고 결정을 기다린다.

- **파일:** `src/App.tsx`, `src/components/Header.tsx`, `src/components/StoryCard.tsx`,
  `src/components/VoteResult.tsx`, `src/components/AIChatView.tsx`,
  `src/components/ApiKeyModal.tsx`(삭제), `docs/userflow.md`(신규), `PRD.md`

### PATCH-026 | 2026-08-20 | 문의 답변 기능 (운영자 화면 + 사용자 확인)

> **요약:** 쌓이기만 하던 문의에 답할 수 있게 했다. 운영자는 전용 화면에서 답변하고, 사용자는 앱에서 답변을 본다.

- **무엇을:**
  - 운영자 개념을 만들었다 (`admins` 테이블). 여기 계정을 넣으면 권한이 생긴다.
  - 마이페이지에 **'문의 관리'** 메뉴 — 운영자에게만 보인다. 답변 대기 건수가 배지로 뜬다.
  - 사용자 쪽 '도움말 및 문의'에 **'내 문의 내역'** — 답변 대기 / 답변 완료 상태와 답변 내용.
- **왜:** PATCH-024에서 문의를 저장하게 만들었지만 **답할 방법이 없었다.** 보낸 사람도
  접수됐는지 알 수 없었다.
- **메일 대신 앱 안에서 주고받는 이유:** 메일 발송 서비스(키)가 없다. 외부 서비스를
  붙이는 건 별도 결정이라, 지금 가능한 방법으로 먼저 닫았다.
- **문구 정정:** 문의 입력칸에 "답변은 가입하신 이메일로 발송됩니다"라고 쓰여 있었으나
  사실이 아니다. "아래 '내 문의 내역'에서 확인" 으로 고쳤다.
- **권한 처리:** `is_admin()` 함수로 판정하고, `admins` 테이블 자체는 아무도 읽지 못하게
  두었다. RLS상 운영자만 전체 문의를 읽고 답변을 쓸 수 있다.
- **확인:** 일반 계정에는 '문의 관리' 메뉴가 안 보임 → 운영자 등록 후 메뉴 노출·대기 배지
  1건 → 답변 작성 → 사용자 화면에서 '답변 완료'와 답변 내용 확인.
- **운영자 계정:** `master@master.master` (운영 전용). 권한 범위와 등록·해제 방법은
  `docs/admin.md` 참고. **배포 전 비밀번호 변경 필요.**
- **남은 것:** 답변이 달려도 사용자가 알 길이 없다. 직접 들어와 봐야 한다. 알림 수단이
  생기면 붙이는 편이 낫다.
- **파일:** `src/lib/inquiries.ts`(신규), `src/components/MyPageView.tsx`,
  마이그레이션 `inquiry_replies_and_admins`

### PATCH-025 | 2026-08-20 | 패치 노트에 한눈에 보기 요약 추가

> **요약:** 패치 목록 맨 위에 한 줄 요약 표를 넣고, 각 항목도 요약 한 줄로 시작하게 했다.

- **무엇을:** §7 맨 위에 '한눈에 보기' 표를 만들고, 기존 24개 항목 전부에
  `> **요약:**` 한 줄을 붙였다.
- **왜:** 항목마다 증상·원인·검증이 길게 적혀 있어서, 무엇이 바뀌었는지 훑어보기가
  어려웠다. 개발자가 아닌 사람이 **표만 읽고도** 흐름을 잡을 수 있어야 한다.
- **표기:** 그냥 두면 서비스가 잘못 동작하거나 개인정보가 새는 문제였던 것에 ⚠️ 를 달았다.
  24개 중 6개가 여기 해당한다.
- **파일:** `PRD.md`, `CLAUDE.md`

### PATCH-024 | 2026-08-20 | 마이페이지 껍데기 UI를 실제 동작으로 (심각)

> **요약:** 눌러도 아무 일도 안 하던 마이페이지 버튼들(비밀번호 변경·계정 탈퇴·문의하기·알림 설정)을 실제 동작으로 연결했다.

- **증상:** 마이페이지 하위 화면 상당수가 **눌러도 아무 일도 안 일어나는 상태**였다.
  `onClick`이 아예 없었다.
  - **비밀번호 변경**: 입력칸 3개 + 버튼, 동작 없음
  - **계정 탈퇴**: 모달·사유 선택까지 다 있는데 실행 없음
  - **문의하기**: 동작 없음
  - **알림 설정 3종**: `useState`만 있고 저장 안 됨 → 화면을 나갔다 오면 되돌아감
  - **이메일**: `a****@gmail.com`이 코드에 하드코딩 → 누가 로그인하든 이 문자열
- **왜 심각한가:** 6개월 자동 삭제까지 만들어 놓고 정작 **계정을 지울 방법이 없었다.**
  앞서 "탈퇴 시 대화도 함께 삭제된다"고 적었던 것도 DB 레벨에서만 맞는 말이었다.
- **고친 방법:**
  - **탈퇴**: `delete_my_account()` 서버 함수 (`security definer`, `auth.uid()`로 본인만).
    `stories`·`comments`·`votes`·`reports`는 외래키가 없어 자동으로 안 지워지므로 직접
    지운다. 내 사연에 달린 **남의 댓글·투표**도 함께 지운다 — 사연이 사라지는데 댓글만
    남으면 어디에도 닿지 않는 데이터가 된다.
  - **비밀번호 변경**: 기존 비밀번호를 한 번 확인한 뒤 변경한다. 세션만 있으면 바꿀 수
    있지만, 자리를 비운 사이 남이 바꾸는 걸 막으려면 확인이 필요하다.
  - **알림 설정**: 계정 메타데이터에 저장. 기기를 바꿔도 따라온다.
  - **문의하기**: `inquiries` 테이블에 쌓는다 (본인 것만 읽기/쓰기, 수정·삭제 불가).
  - **이메일**: 세션에서 읽어 가려서 표시한다.
- **곁들여 고친 것:** 성공 메시지가 **요약 화면에서만** 렌더돼서, 비밀번호를 바꿔도
  화면에 아무 표시가 없었다. 각 폼 안에서 알리도록 했다.
- **문구 정정:** 탈퇴 모달에 "30일 후 완전 파기"라고 쓰여 있었으나 그런 처리는 없다.
  즉시 삭제된다는 사실과, 내 사연에 달린 남의 댓글도 사라진다는 점을 명시했다.
- **확인:** 전용 계정을 만들어 사연을 등록한 뒤 화면에서 탈퇴 → **계정 0건, 그 사연 0건,
  다른 사연 11건·댓글 22건은 그대로.** 비밀번호는 틀린 기존 비번/불일치/6자 미만 모두
  차단되고 실제 변경도 확인(변경 후 원복). 알림 토글은 새로고침 후에도 유지.
- **남은 것:** 문의는 쌓이기만 하고 답변 수단이 없다. 운영자가 볼 화면이나 메일 발송이
  필요하다.
- **파일:** `src/components/MyPageView.tsx`, 마이그레이션 `delete_my_account`·`create_inquiries`

### PATCH-023 | 2026-08-20 | 6개월 지난 AI 대화 자동 삭제

> **요약:** 마지막 대화로부터 6개월이 지난 AI 대화방을 매일 새벽 자동으로 지운다.

- **정책:** **마지막으로 대화한 지 6개월**이 지난 대화방은 자동으로 지운다.
  기준을 '만든 날'이 아니라 `updatedAt`으로 잡은 이유는, 오래 쓰던 대화방이 만든 지
  오래됐다는 이유로 사라지면 안 되기 때문이다.
- **구현:** `pg_cron`으로 매일 새벽 4시(한국 시간, UTC 19시)에
  `public.purge_stale_ai_personas()`를 돌린다. 함수는 `security definer`이고 실행 권한을
  `anon`/`authenticated`에서 회수해 두었다.
- **화면에 명시:** 마이페이지 'AI 대화 전체 삭제' 카드에 "마지막으로 대화한 지 6개월이
  지난 대화방은 자동으로 지워집니다"를 적었다. **말없이 지우면 안 된다**는 것이
  PATCH-022에서 자동 삭제를 미뤘던 이유였고, 그 문제를 이렇게 처리했다.
- **확인:** 7개월 전 / 5개월 전 / 오늘 대화 3건을 넣고 실행 → **7개월 것 1건만 삭제**,
  나머지 2건 유지. `cron.job`에 `active = true`로 등록 확인.
- **아직 없는 것:** 지워지기 전에 미리 알려주는 수단(푸시·메일)이 없다. 화면 안내가
  전부다. 알림 기능이 생기면 "곧 지워질 대화" 안내를 붙이는 편이 낫다.
- **파일:** 마이그레이션 `purge_stale_ai_personas`, `src/components/MyPageView.tsx`

### PATCH-022 | 2026-08-20 | AI 대화 보관 정책 (전체 삭제 제공, 자동 삭제 없음)

> **요약:** 마이페이지에서 AI 대화를 한 번에 전부 지울 수 있게 했다. 자동 삭제는 이때 넣지 않았다.

대화가 서버에 남게 되면서(PATCH-021) 정해야 했던 보관 정책을 이렇게 잡았다.

- **사용자가 직접 지운다.** 마이페이지에 'AI 대화 전체 삭제'를 뒀다. 대화방을 하나씩
  지우게 하면 정작 급할 때 다 못 지운다. 되돌릴 수 없으므로 확인 단계를 한 번 둔다.
- **탈퇴하면 함께 지워진다.** `auth.users` 삭제 시 연쇄 삭제된다(PATCH-021에서 이미 걸었음).
- **자동 삭제는 넣지 않았다.** 일정 기간 뒤 말없이 지우면, 사용자가 다시 보려던 기록이
  예고 없이 사라진다. 지금은 실제 사용자가 없어 보관 비용도 문제가 아니다. 나중에
  필요해지면 `pg_cron` 한 줄로 붙일 수 있고, 그때는 **미리 알리는 방법**을 함께 정해야 한다.

- **확인:** 대화 2개 → '전부 지우기' → 확인 단계 노출 → 실행 후 DB 0건, AI 탭 빈 상태,
  안내 문구가 "지울 대화가 없어요"로 바뀜.
- **파일:** `src/lib/aiPersonas.ts`, `src/components/MyPageView.tsx`, `src/App.tsx`

### PATCH-021 | 2026-08-20 | AI 대화방을 서버로 옮김 (브라우저 저장 폐지)

> **요약:** AI 대화를 브라우저가 아니라 계정(서버)에 저장하도록 옮겨, 기기를 바꿔도 따라오게 했다.

- **무엇을:** AI 대화방을 Supabase `ai_personas` 테이블로 옮겼다. **브라우저에는 대화를
  일절 저장하지 않는다.** 기기에 남아 있던 예전 키도 앱을 열 때 지운다.
- **왜:** PATCH-020으로 계정 간 유출은 막았지만 대화가 여전히 기기에 묶여 있었다.
  휴대폰에서 나눈 대화가 PC에서 안 보이고, 브라우저 저장소를 지우면 사라졌다.
  이 서비스는 휴대폰과 PC를 오가며 쓰는 것을 전제로 한다.
- **구조:**
  - 읽고 쓰는 창구는 `src/lib/aiPersonas.ts` 하나뿐 (`aiQuota.ts`와 같은 방식)
  - 대화 내용은 `chatHistory` jsonb 한 칸. 통째로 갈아끼우는 기존 흐름과 맞다
  - RLS는 SELECT·INSERT·UPDATE·DELETE 모두 **본인 행만**. 사용 기록(`ai_chat_usage`)과
    달리 삭제를 열어둔 이유는, 내가 털어놓은 이야기를 내가 지울 수 없으면 안 되기 때문
- **주의해서 처리한 것:** AI 답변은 한 글자씩 흘러 들어오고 그때마다 저장 경로가 불린다.
  그대로 두면 **글자 수만큼 DB 쓰기**가 나가므로 800ms 디바운스로 묶었다.
- **확인:**
  - 기기 A에서 대화 → **한 번도 쓴 적 없는 새 브라우저**에서 같은 계정으로 로그인 →
    대화 내용까지 그대로 보임 (브라우저 저장 0건)
  - DB에서 직접 확인: 본인 계정 1개 / 다른 계정 **0개**
  - 로그아웃 시 대화방 안 보이고 AI 탭 차단, 이전 계정 닉네임 흔적 없음
- **남은 결정:** 대화 내용이 서버에 남는 만큼 **보관 기간과 삭제 정책**을 정해야 한다.
  지금은 사용자가 지우기 전까지 계속 남는다.
- **파일:** `src/lib/aiPersonas.ts`(신규), `src/App.tsx`, 마이그레이션 `create_ai_personas`

### PATCH-020 | 2026-08-20 | 로그아웃·계정 전환 시 남의 AI 대화가 보이던 문제 (심각)

> **요약:** 같은 브라우저에서 다른 계정으로 로그인하면 앞사람의 대화가 보이던 문제를 막았다.

- **증상:** AI 대화방이 계정 구분 없이 브라우저 한 곳에 저장돼서,
  1. **로그아웃해도 대화방이 그대로 남았다.** 내가 시작하지도 않은 대화가 목록에 있다.
  2. **같은 브라우저에서 다른 계정으로 로그인하면 앞사람의 대화방과 그 안의 대화 내용이
     그대로 보였다.** 헤더 닉네임까지 이전 계정 것으로 남아 있었다.
  3. 로그아웃 뒤에는 게스트 제약도 걸리지 않아 AI 탭에 그냥 들어가졌다.
- **재현:** A 계정으로 대화를 만들고 "이건 A 계정의 비밀 이야기입니다"를 보낸 뒤 로그아웃 →
  B 계정으로 로그인 → B의 화면에서 A의 대화방과 그 문장이 그대로 확인됨.
- **왜 심각한가:** 익명으로 속마음을 털어놓는 서비스다. 공용 PC나 가족 공용 기기라면
  다음 사람이 앞사람의 가장 사적인 대화를 그대로 보게 된다.
- **고친 방법:**
  - 저장 키를 계정별로 나눴다 (`nipyeon_personas_v2:<userId>`). 계정 구분이 없던 시절의
    키는 발견 즉시 지운다.
  - 로그인 여부는 `authUserId`(세션에서 온 값)로만 판단한다. `user.id`는 로그인 전에도
    임시값이 들어 있어 근거가 못 된다.
  - 로그아웃 시 대화방·진행 중 세션·닉네임을 모두 비우고, 게스트 제약(`isGuest`)을 켜
    투표·AI 대화가 막히게 했다.
- **확인:** 로그아웃 시 대화방 0개 + AI 탭 진입 시 "AI와 대화하려면 로그인이 필요해요",
  이전 계정 닉네임 사라짐, 다시 로그인하면 **내 대화방만** 복구, 다른 계정 키를 심어두고
  확인해도 읽히지 않음.
- **남은 한계:** 대화방은 여전히 그 브라우저에만 있다. 기기를 바꾸면 따라오지 않고,
  브라우저 저장소를 지우면 사라진다. 계정을 따라다니게 하려면 서버로 옮겨야 한다.
- **파일:** `src/App.tsx`

### PATCH-019 | 2026-08-20 | 기본 제공 페르소나 제거 + 빈 상태 화면

> **요약:** 사연과 무관한 데모 페르소나 3종을 없애고, 대신 빈 화면 안내를 넣었다.

- **무엇을:** 미리 깔려 있던 데모 캐릭터 3종(김부장·민우·지은)을 없앴다. 이제 페르소나는
  **사연에서 대화를 열었을 때만** 생긴다.
- **왜:** 이 서비스의 AI 대화는 '내 사연 속 그 사람'과 나누는 것이 핵심인데, 사연과 아무
  관련 없는 캐릭터가 먼저 보이면 그 연결이 흐려진다.
- **곁들여:** 기본값이 없어지면 처음 온 사람에게 이 탭이 텅 빈다. 고장 난 것처럼 보이지
  않도록 빈 상태 화면을 만들었다 — 대화가 어디서 시작되는지 알려주고 '사연 보러 가기'로
  곧장 피드로 보낸다.
- **파일:** `src/data/mockData.ts`, `src/App.tsx`, `src/components/AIChatView.tsx`

### PATCH-018 | 2026-08-20 | 페르소나 저장소 전면 초기화 (v2)

> **요약:** 이름 규칙이 바뀌기 전에 만들어진 페르소나를 전부 비웠다.

- **무엇을:** 페르소나 저장 키를 `nipyeon_personas` → `nipyeon_personas_v2`로 올려,
  **변경 전에 만들어진 페르소나를 모두 버린다.** 예전 키는 지운다.
- **왜:** 이름 규칙이 바뀌기 전(PATCH-007) 만들어진 공감 카드는 이름이 사연 제목이라
  새 호칭과 섞여 목록이 뒤죽박죽이었고, **이미 사라진 사연을 가리키는 옛 카드**까지
  남아 있었다. 특정 조건만 골라 거르는 것보다 한 번 깨끗이 비우는 편이 낫다고 봤다.
- **주의:** 예전 카드에 쌓인 대화 내용도 함께 사라진다. 되돌릴 수 없다.
- **앞으로:** 페르소나 이름 규칙이나 구조를 또 바꾸면 키 뒤 번호를 올린다.
- **한계:** 페르소나는 서버가 아니라 브라우저(localStorage)에 있다. 그래서 "계정별
  초기화"가 불가능하고, 각 브라우저가 앱을 한 번 열어야 정리된다.
- **파일:** `src/App.tsx`

### PATCH-017 | 2026-08-20 | AI 대화 화면이 한 화면에 안 들어가던 문제

> **요약:** 입력창이 하단 메뉴에 가리던 AI 대화 화면 높이를 실제 여백에 맞췄다.

- **증상:** 대화방에서 **입력창이 하단 네비게이션에 가렸다.** 타자를 치려면 페이지 전체를
  스크롤해야 했고, 스크롤하면 대화 헤더가 상단 헤더 밑으로 말려 들어가 화면이 깨져 보였다.
  (문서 높이 885px / 뷰포트 806px)
- **원인:** 대화 컨테이너 높이가 `h-[82vh]`라는 어림값이었고 `mb-24`까지 붙어 있었다.
  상단 헤더(64px)·`main`의 상하 패딩(각 32px)·하단 네비게이션(80px)이 계산에 없었다.
- **고친 방법:** `h-[calc(100dvh-184px)]`로 바꾸고 `mb-24`를 없앴다(헤더 64 + 패딩 32 +
  네비 80 = 176, 여백 8). `vh` 대신 `dvh`를 써서 모바일 주소창이 접히고 펴져도 맞는다.
- **확인:** 문서 높이 806 = 뷰포트 806(페이지 스크롤 없음), 입력창 아래 701 < 네비 위 726.
- **파일:** `src/components/AIChatView.tsx`

### PATCH-016 | 2026-08-20 | 사연 쓰기 버튼이 다른 탭까지 따라다니던 문제

> **요약:** 사연 쓰기 플로팅 버튼을 피드 탭에서만 띄운다. AI 탭에서 다른 버튼을 덮고 있었다.

- **증상:** 모바일 플로팅 '사연 쓰기' 버튼이 AI 대화 탭과 마이 탭에도 떠 있었다. 그 탭과
  무관한 동작인 데다, AI 대화 탭에서는 **페르소나 카드의 '시작하기' 버튼을 실제로 덮고
  있었다**(좌표로 확인).
- **고친 방법:** 피드 탭에서만 렌더한다. `aria-label`도 함께 붙였다.
- **파일:** `src/App.tsx`

### PATCH-015 | 2026-08-20 | 아이콘만 남는 버튼에 이름 부여

> **요약:** 좁은 화면에서 이름이 사라지던 헤더 버튼과 카드 숫자에 읽을 이름을 붙였다.

- **증상:** 헤더의 사연 등록 버튼이 좁은 화면에서 '사연 등록' 글자를 숨기는데, 그러면
  아이콘만 남아 **이름이 아예 없는 버튼**이 된다. 스크린리더가 "버튼"이라고만 읽는다.
  사연 카드 하단의 댓글·투표 수도 맥락 없이 "0", "45"로만 읽혔다.
- **고친 방법:** 헤더 버튼에 `aria-label`, 카드 숫자 앞에 `sr-only` 라벨(댓글/투표)을 붙였다.
- **확인:** 접근성 트리에서 헤더 버튼이 "사연 등록"으로 읽힘.
- **참고:** 하단 네비게이션(Home/Ai 대화/마이)은 이미 정상이었다. 아이콘 합자에
  `aria-hidden`이 붙어 있다.
- **파일:** `src/components/Header.tsx`, `src/components/StoryCard.tsx`

### PATCH-014 | 2026-08-20 | 공유 링크로 들어오면 이미 한 투표가 안 보이던 문제

> **요약:** 공유 링크로 들어오면 이미 한 투표가 안 보이던 문제를 고쳤다.

- **증상:** 공유 링크(`?story=...`)로 들어오면 **이미 투표한 사연인데도 "투표하면 결과가
  열려요"** 가 뜨고 결과 그래프와 공유 바가 나오지 않았다. 뒤에 깔린 목록 카드는 결과를
  제대로 보여주고 있어서 더 헷갈린다. 9초를 기다려도 그대로였다.
- **원인:** 딥링크 핸들러가 사연 목록이 채워지자마자 실행되는데, 그 시점은 내 투표 기록이
  아직 반영되기 전이다. 상세 모달은 그때의 **스냅샷**을 복사해 쓰고, 이후 목록이 갱신돼도
  따라가지 않는다.
- **고친 방법:** 열려 있는 상세를 목록의 최신 값과 맞추는 effect를 뒀다. 값이 그대로면
  이전 객체를 그대로 돌려줘 불필요한 렌더를 만들지 않는다.
- **확인:** 딥링크 진입 시 7%/93% 결과·"투표는 한 번만 바꿀 수 있어요"·공유 바 모두 정상.
  3초간 localStorage 쓰기 0회(렌더 루프 없음).
- **파일:** `src/App.tsx`

### PATCH-013 | 2026-08-20 | 공유 카드에서 한쪽 비율이 작을 때 퍼센트가 안 읽히던 문제

> **요약:** 한쪽 비율이 작을 때 퍼센트 숫자가 안 읽히던 공유 카드를 고쳤다.

- **증상:** 한쪽 표가 적으면(예: 12% 대 88%) 그 숫자가 자기 칸을 넘쳐 **반대편 막대 위에
  자기 색으로** 찍혔다. 파란 글자가 코랄 막대 위에 얹혀 거의 안 보였다.
- **고친 방법:** 넘칠 때는 얹히는 바탕색에 맞춰 글자색을 고른다(코랄 위에는 잉크,
  파랑 위에는 흰색 — 각 막대가 원래 쓰는 글자색과 같다).
- **확인:** 12:88, 91:9 양쪽 극단으로 카드를 뽑아 육안 확인.
- **파일:** `src/lib/shareCard.ts`

### PATCH-012 | 2026-08-20 | 신고 모달을 PRD 명세대로 (사유 선택 + 디자인 토큰)

> **요약:** 신고 사유를 고를 수 있게 만들고 디자인을 서비스 토큰에 맞췄다.

- **증상:** "신고 사유를 정확하게 선택해 주세요"라고 안내해 놓고 **고를 것이 없었다.**
  자유 입력 칸 하나가 전부였고 `required`라서, 비워두고 제출하면 아무 반응 없이 막혔다.
  게다가 이 모달만 크림/골드(`#fffaf0`·`#f5f0e0`·`rounded-3xl`)로 서비스와 따로 놀았다.
- **고친 방법:** PRD FEAT-09 명세대로 사유 5종(욕설·비방 / 음란·선정성 / 광고·도배 /
  개인정보 노출 / 기타)을 라디오로 넣고, 상세 입력은 선택 사항으로 돌렸다('기타'만 필수).
  사유를 고르기 전에는 제출 버튼이 비활성이다. 색은 서비스 토큰(`#A32E1D`·`#E5E7EB`·
  `#f8f9fa`)으로 맞췄다.
- **곁들여:** `useState`가 조기 반환(`if (!isOpen) return null`) **아래**에 있었다. 지금은
  터지지 않지만 훅 순서가 렌더마다 달라지는 구조라 위로 올렸다.
- **확인:** 사유 미선택 시 비활성 → 선택 시 활성 → `submit_report` RPC 200
  (`{blinded:false, reports:1}`) → 접수 완료 안내.
- **파일:** `src/components/ReportModal.tsx`

### PATCH-011 | 2026-08-20 | 댓글 공감이 서버에 저장되지 않던 문제

> **요약:** 댓글 공감이 화면에서만 오르고 서버에 저장되지 않던 문제를 고쳤다.

- **증상:** 투표(PATCH-008)와 **똑같은 원인**. 공감 수가 화면에서만 오르고
  `like_comment` RPC가 한 번도 호출되지 않았다.
- **원인:** `handleLikeComment`가 `updateNeeded`·`delta`를 `setCommentsMap` 콜백 안에서
  세우고 콜백 바깥에서 읽었다. 그 콜백은 렌더 시점에 실행되므로 읽는 순간엔 늘 초기값
  (`false` / `0`)이었다.
- **고친 방법:** 대상 댓글을 현재 상태에서 먼저 찾아 증감치를 정하고, 그 다음에 화면과
  서버를 갱신한다.
- **확인:** `like_comment` RPC 200, DB `comments.likeCount` 증가 확인.
- **파일:** `src/App.tsx`

### PATCH-010 | 2026-08-20 | 파비콘·메타 태그 추가 (콘솔 에러 제거)

> **요약:** 파비콘과 메타 태그를 넣어 매 접속마다 나던 콘솔 404를 없앴다.

- **증상:** 모든 페이지 로드마다 콘솔에 404가 하나씩 찍혔다. 브라우저가 기본으로
  `/favicon.ico`를 찾는데 `index.html`에 **아이콘 링크가 하나도 없었다.** 탭 아이콘도
  비어 있었다.
- **고친 방법:** 서비스 토큰(내 편 `#FF6B5A` / 니 편 `#6C7BE8` / 잉크 `#1C1C1C`)으로
  `public/favicon.svg`를 만들어 연결하고, `theme-color`와 `description` 메타를 넣었다.
- **결과:** 콘솔 에러 0, 실패 요청 0.
- **파일:** `index.html`, `public/favicon.svg`(신규)

### PATCH-009 | 2026-08-20 | 무료 횟수 조회 방식 변경

> **요약:** 콘솔에 계속 오류를 남기던 무료 횟수 조회 방식을 바꿨다.

- **증상:** 무료 횟수를 세는 요청이 매번 콘솔에 `404` / `ERR_ABORTED`를 남겼다. 값은
  정상적으로 왔지만 콘솔이 늘 지저분했다.
- **원인:** Supabase count 조회에 `head: true`(HEAD 요청)를 썼다.
- **고친 방법:** 행이 하루 몇 개뿐이라 그냥 받아서 센다.
- **파일:** `src/lib/aiQuota.ts`

### PATCH-008 | 2026-08-20 | 투표가 서버에 저장되지 않던 문제 (심각)

> **요약:** 투표가 화면에만 반영되고 서버에 한 건도 저장되지 않던 문제를 고쳤다.

- **증상:** 투표하면 화면에는 결과 그래프가 열리는데 **DB `votes` 테이블에 한 건도
  남지 않았다.** 새로고침하면 표가 사라진다. 에러 토스트도 안 떠서 겉보기엔 정상이었다.
- **원인:** `handleVote`가 "서버로 보낼 필요 있음" 플래그를 `setStories` 콜백 **안에서**
  세우고, 콜백 바깥 바로 다음 줄에서 읽었다. React는 그 콜백을 렌더 시점에 실행하므로
  읽는 순간에는 늘 `false`였고, 투표 RPC 호출 직전에 return돼 버렸다.
- **고친 방법:** 판정(최초 투표인지 / 변경인지 / 변경 한도 초과인지)을 현재 상태에서
  먼저 하고, 그 다음에 화면 갱신과 서버 호출을 한다.
- **확인:** 투표 후 `vote_story` RPC 200 응답, `votes` 행 생성, `stories.votesB` 증가 확인.
- **파일:** `src/App.tsx`

### PATCH-007 | 2026-08-20 | 공감 모드 대화 상대 이름이 사연 제목으로 나오던 문제

> **요약:** 공감 모드 말풍선 발신자가 사연 제목으로 찍히던 문제를 고쳤다.

- **증상:** 공감 모드 말풍선의 발신자가 **사연 제목**("명절마다 차별하시는 부모님")으로
  찍혔다. 위로해주는 친구가 아니라 사연 속 갈등 상대가 말하는 것처럼 보였다.
- **고친 방법:** 비율별 호칭(내 편 친구 / 들어주는 친구 / 짚어주는 친구)을
  `prompts.ts`의 `EMPATHY_PERSONA_NAMES`로 두고, 사연 제목은 상황 모드와 같이
  카드 설명으로 옮겼다(목록에서 어느 사연인지 구분하려면 제목이 필요하다).
- **곁들여:** 공감 비율 설정 모달에 제목이 두 번 찍히던 것 정리.
- **파일:** `src/lib/prompts.ts`, `src/App.tsx`, `src/components/AIExplainSettingsModal.tsx`

### PATCH-006 | 2026-08-20 | 대화 상대 호칭 뒤 조사(와/과) 교정

> **요약:** 대화 상대 호칭 뒤 조사를 받침에 맞춰 고르게 했다.

- **무엇을:** 시작점 선택 화면의 "OO와의 대화를…" 문구에서 조사를 받침에 맞춰 고르도록
  `gwaWa()` 헬퍼를 붙였다.
- **왜:** 호칭이 카테고리마다 달라진다(연인·직장 상대·친구·가족·상대방). '와'로 고정돼
  있어서 **"상대방와의 대화", "연인와의 대화"** 가 실제로 화면에 나가고 있었다.
- **파일:** `src/components/AIChatModeSelectionModal.tsx`

### PATCH-005 | 2026-08-20 | 패치 노트 규칙 문서화

> **요약:** 커밋마다 이 패치 노트를 남기는 규칙을 CLAUDE.md에 적었다.

- **무엇을:** 커밋마다 이 §7에 항목을 남기는 규칙을 `CLAUDE.md`(신규)에 적었다.
- **왜:** 팀원이 코드를 다 훑지 않고도 지금까지 뭐가 바뀌었는지 이 파일 하나로
  파악할 수 있어야 한다.
- **파일:** `CLAUDE.md`(신규), `PRD.md`

### PATCH-004 | 2026-08-20 | 내편지수(empathyScore) 제거

> **요약:** 화면에 쓰이지 않고 공감 비율과 중복이던 내편지수를 제거했다.

- **무엇을:** 대화 세션이 들고 있던 `empathyScore` 필드를 코드에서 걷어냈다.
- **왜:** 공감 비율(내 편 100% / 반반 / 상대편 100%)을 숫자로 베낀 값에 불과했고
  (High→90, Middle→50, Low→10 고정), **화면 어디에도 표시되지 않았다.** 게다가 이 값을
  App과 채팅 화면이 서로 주고받다가 무한 렌더 루프를 냈던 전력이 있다.
- **영향:** §6 정보구조도의 "내편지수 연출" 항목이 빠졌다. 대화 중 상대가 얼마나
  누그러졌는지 보여주는 게이지가 필요해지면, 그때 AI가 매 턴 점수를 내도록
  프롬프트부터 다시 설계해야 한다 (지금 구조로는 못 만든다).
- **파일:** `src/types.ts`, `src/App.tsx`, `src/components/AIChatView.tsx`

### PATCH-003 | 2026-08-20 | AI 무료 횟수를 서버에서 카운트

> **요약:** 브라우저만 지우면 초기화되던 AI 무료 횟수를 서버에서 세도록 옮겼다.

- **무엇을:** 하루 3회 무료 제한을 localStorage → Supabase `ai_chat_usage` 테이블로 옮겼다.
- **왜:** 브라우저 저장소만 지우면 횟수가 무한히 초기화됐다. 나중에 실제 과금을 붙이려면
  그 숫자를 사용자 기기가 아니라 서버가 들고 있어야 한다.
- **정책:** 하루 경계는 **한국 시간(Asia/Seoul)** 기준. 내가 쓴 사연은 횟수를 쓰지 않고,
  이미 열었던 대화에 다시 들어가도 차감하지 않는다.
- **보안:** RLS에 SELECT·INSERT만 본인 것으로 열어두고 **UPDATE·DELETE 정책은 만들지 않았다.**
  사용 기록을 지워서 횟수를 되돌릴 수 없다 (실제로 삭제가 막히는지 확인함).
- **한계:** 로그인 전이거나 서버가 응답하지 않으면 예전처럼 로컬에서 센다. 과금이 붙으면
  이 폴백은 다시 봐야 한다.
- **파일:** `src/lib/aiQuota.ts`(신규), `src/App.tsx`, DB 마이그레이션 `create_ai_chat_usage`

### PATCH-002 | 2026-08-20 | 대화 종결 기호를 상수 하나로 통합

> **요약:** 양쪽에 하드코딩돼 있던 대화 종결 기호를 상수 하나로 모았다.

- **무엇을:** `[SIM_END:SUCCESS]` / `[SIM_END:FAIL]` 문자열을 `src/lib/prompts.ts`의
  `SIM_END` 상수로 모으고, 판별·제거 헬퍼(`detectSimEnd`, `stripSimEnd`)를 함께 뒀다.
- **왜:** 기호를 붙이라고 시키는 쪽(프롬프트)과 읽고 지우는 쪽(채팅 화면)에 따로
  적혀 있었다. 한쪽만 고치면 **대화가 영영 안 끝나거나 기호가 화면에 그대로 새어 나간다.**
- **파일:** `src/lib/prompts.ts`, `src/components/AIChatView.tsx`

### PATCH-001 | 2026-08-19 | AI 프롬프트 통합 및 난이도 상향

> **요약:** 두 벌로 갈라져 있던 AI 프롬프트를 한 곳으로 모으고 화해 난이도를 올렸다.

- **무엇을:** App.tsx에 두 벌 복붙돼 있던 대화 프롬프트를 `src/lib/prompts.ts` 한 곳으로
  모으고, 화해 판정 조건을 엄격하게 바꿨다. 무료 체험(하루 3회)과 페이월도 이때 붙었다.
- **왜:** 두 벌이 이미 서로 달라져서 **같은 사연이라도 어디서 열었느냐에 따라 AI가 다르게
  굴었다.** 또 화해 조건이 OR 3개라 두 턴 만에 상대가 무너져서, 대화 연습이 아니라
  기분 좋은 판타지가 되고 있었다.
- **자세히:** `docs/ai-prompts.md`

---

## 8. 앞으로 볼 것 (아이디어)

### 삭제 예정 대화 미리 알리기

PATCH-023으로 6개월 자동 삭제가 들어갔다. 지금 사용자가 이 정책을 알 수 있는 곳은
마이페이지 안내 문구 하나뿐이다.

알림 기능이 생기면 "○○ 대화가 곧 지워집니다"를 미리 보내는 편이 낫다. 그때
'지금 이어서 대화하기'로 연결하면 삭제를 미룰 수도 있다(대화하면 `updatedAt`이 갱신된다).

### 큐레이션 페르소나 (인기 드라마 주인공 등)

지금 페르소나는 전부 사용자의 사연에서 만들어진다. 여기에 **서비스가 미리 준비한
페르소나**(예: 인기 드라마 주인공)를 얹는 방향을 생각해 볼 만하다. 사연을 쓰기 전에도
써 볼 수 있는 입구가 되고, 화제성 있는 캐릭터는 유입에도 도움이 된다.

다룰 때 정해야 할 것:

- 사연에서 만들어진 페르소나와 **목록에서 구분**해서 보여줄 것 (섞이면 '내 사연 속 그
  사람'이라는 핵심이 흐려진다 — PATCH-019에서 데모 캐릭터를 걷어낸 이유가 그것이다)
- 무료 횟수를 쓰게 할지, 별도로 열어줄지
- 실존 작품·인물을 쓸 때의 권리 문제
